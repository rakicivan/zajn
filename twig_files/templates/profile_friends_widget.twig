<div class="col-xs-3">
    <div class="row">
        <div class="user_avatar">
            <a href="{{base_url}}/profile.php?id={{user_ID}}"><img class="img-circle center-block" src="{{user_avatar}}" data-toggle="tooltip" data-placement="bottom" title="{{user_name}} {{user_surname}}"></a>
        </div>
    </div>
</div>